CREATE OR REPLACE VIEW V_EDIFICIO AS
SELECT 'EDI'||EDI.OID AS OID,
       EDI.DESCRIZIONE,
       EDI.DESCRIZIONE AS NAME,
       EDI.OID AS EDIFICIO,
       'CLED'||EDI.CLUSTEREDIFICI AS CLUSTEREDIFICI

FROM EDIFICIO EDI
WHERE EDI.CLUSTEREDIFICI IS NOT NULL;