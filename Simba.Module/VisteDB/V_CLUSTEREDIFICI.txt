CREATE OR REPLACE VIEW V_CLUSTEREDIFICI AS

SELECT 'CLED'||CLED.OID AS OID,
       CLED.DESCRIZIONE,
       CLED.DESCRIZIONE AS NAME,
       'SCEN'||CLED.SCENARIO AS SCENARIO,
       CLED.OID AS CLUSTEREDIFICI

FROM CLUSTEREDIFICI CLED 
WHERE CLED.SCENARIO IS NOT NULL